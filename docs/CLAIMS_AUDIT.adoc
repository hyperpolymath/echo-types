// SPDX-License-Identifier: MPL-2.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= Claims Audit — Gate G1 input
:toc: macro
:toclevels: 2
:sectnums:
:sectnumlevels: 2
:icons: font

[.lead]
Mapping-only inventory of claim-bearing sentences on every user-facing
surface, prepared as input to Gate G1 (`roadmap-gates.adoc`
§"Gate 1: Distinct phenomenon"). No claim here is judged justified or
unjustified; the rows pair each surface sentence with a proposed
`(module, theorem)` mapping or the literal token `NONE-PROPOSED` when
no in-repo artefact corresponds.

toc::[]

== Scope and absent surfaces

Surfaces in scope per the constructor brief:

* paper abstract + introduction (`docs/echo-types/paper.adoc`, the
  status `[IMPORTANT]` block and §1 "Introduction")
* TYPES extended abstract (`docs/echo-types/types-abstract.adoc`,
  whole document)
* `README.md` (repo root)
* GitHub repo description (retrieved via `gh repo view
  hyperpolymath/echo-types`)
* card strings in `build-card.mjs`
* LinkedIn draft copy

Surfaces *not located* in any branch, worktree, or sibling repo
inspected:

[cols="1,3", options="header"]
|===
| Surface | Search outcome

| `docs/CLOSING_SPRINT.adoc`
| Not present on `main`, the current `session/slice-4-narrowing`
  branch, or any other local branch. Gate G1 reference text taken
  from `roadmap-gates.adoc` §"Gate 1: Distinct phenomenon" instead.

| `build-card.mjs`
| Not present anywhere under `/home/hyperpolymath`. The only `.mjs`
  in this repo is `tools/banner/build-banner.mjs` (a banner
  generator with no claim-bearing card strings, audited and
  excluded as out of scope).

| LinkedIn draft copy
| No `*linkedin*` / `*draft*` file references echo-types under the
  search paths inspected. Recorded as `LOCATE-FAILED` so a later
  session can supply it without disturbing the rest of the table.
|===

Within the in-scope surfaces, sentences are extracted verbatim
(modulo whitespace normalisation). Punctuation and special tokens
are preserved. The TYPES extended abstract is included under the
"paper abstract" heading because the repo treats it as Pillar E's
"artefact 1" (`establishment-plan.adoc`) — i.e. the document the
paper's own abstract grows into.

== Methodology

* One row per claim-bearing sentence. Restatements are kept as
  separate rows so the surface multiplicity is visible.
* `Surface` column: short token. `paper-front` = the `[IMPORTANT]`
  block at the top of `paper.adoc`. `paper-intro` = §1 Introduction
  including the Contributions list. `abstract-*` = sections of
  `types-abstract.adoc`. `readme-*` = sections of `README.md`.
  `gh-desc` = the GitHub repository description.
* `Mapping` column: `module.theorem` or `module` when the claim is
  about a whole module / structural artefact. `NONE-PROPOSED` when
  no in-repo artefact corresponds. Multi-module mappings are joined
  with `+`.
* `Note` column: structural observations only — not judgements of
  whether the claim is justified. Each row is mapping-only per the
  brief.

== Surface claim inventory

[cols="1,1,4,3,3", options="header"]
|===
| ID | Surface | Verbatim sentence | Proposed (module, theorem) mapping | Note

| S-001
| gh-desc
| Constructive Agda library for Echo Types: a formal model of structured loss (non-total erasure).
| `Echo.Echo` + repo-level `--safe --without-K` invariant
| Repository-level claim; the per-file invariant is enforced by `scripts/check-no-unsafe.sh` and CI.

| S-002
| gh-desc
| Mechanized Bachmann-Howard ordinals, Buchholz/Veblen hierarchies, and thermodynamic stability proofs.
| `Ordinal.Buchholz.*` + `EchoThermodynamics`
| Spans Lane 3 (Ordinal track, currently 11/13 constructors closed under WfCNF) and the EchoFiberCount-backed Landauer/Bennett family in `EchoThermodynamics.agda`.

| S-003
| readme-lead
| Constructive Agda development for echo types as a first-class notion of structured loss: loss that is not total erasure.
| `Echo.Echo` + `EchoResidue` + `EchoNoSectionGeneric`
| Restates S-001; "first-class notion" is a positioning claim, not a theorem.

| S-004
| readme-core-idea
| Echo types target a third case: irreversible, but with a retained proof-relevant constraint on what was lost.
| `EchoCharacteristic.visible-constraint` + `EchoResidue.echo-to-residue`
| "Proof-relevant constraint" maps to the un-truncated witness component of `Echo`.

| S-005
| readme-core-idea
| This repository treats that third case as the primary object of study.
| NONE-PROPOSED
| Scope/positioning statement; no theorem corresponds.

| S-006
| readme-structural
| This is *the* structural fact the repo establishes: every irreversible `f` factors canonically through its total Echo space, with the left leg an equivalence (the *slogan-unlock*) and the right leg a projection.
| `EchoTotalCompletion.A↔ΣEcho` + `EchoOrthogonalFactorizationSystem.ofs-witness`
| Triangle commutativity is `echo-factorisation` (K-free, pointwise) and `EchoOFSUnivF5.echo-factorisation-strict` (funext-qualified).

| S-007
| readme-structural
| Under `--safe --without-K` the factorisation existence + fibre identification (`ofs-witness`) are unconditional; the function-level universal-property clauses are earned back under Pillar F Gate F5 (`funext` as explicit module parameter, never a postulate).
| `EchoOrthogonalFactorizationSystem.ofs-witness` + `EchoOFSUnivF5` + `EchoOFSUnivF5Diag` + `EchoOFSUnivF5Iso`
| Decomposition: existence + fibre id = K-free; F5-1/2/3 = funext-qualified.

| S-008
| readme-foundation
| Given `f : A → B`, define the fiber/echo at `y : B`: `Echo f y := Σ (x : A) , (f x ≡ y)`.
| `Echo.Echo`
| Definitional; the source-of-truth declaration.

| S-009
| readme-foundation
| with constructive proofs (`--safe --without-K`, no postulates in `proofs/agda`)
| repo invariant (`scripts/kernel-guard.sh`, `scripts/check-no-unsafe.sh`)
| Build-invariant claim; not a single theorem.

| S-010
| readme-foundation
| `echo-intro` (introduction into own fiber)
| `Echo.echo-intro`
| Direct symbol reference.

| S-011
| readme-foundation
| `map-over` (action on fibers for morphisms over fixed base)
| `Echo.map-over`
| Direct symbol reference.

| S-012
| readme-foundation
| `map-over-id` (identity law)
| `Echo.map-over-id`
| Direct symbol reference.

| S-013
| readme-foundation
| `map-over-comp` (composition law)
| `Echo.map-over-comp`
| Direct symbol reference.

| S-014
| readme-foundation
| `map-square` (action along commuting squares)
| `Echo.map-square`
| Direct symbol reference.

| S-015
| readme-status
| full suite compiles: `agda -i proofs/agda proofs/agda/All.agda`
| `All.agda` (aggregator) + CI `.github/workflows/agda.yml`
| Aggregator-build claim. The `for f in proofs/agda/*.agda …` extension also exercises Ordinal/Buchholz tree.

| S-016
| readme-status
| core echo/fiber laws are smoke-pinned (`echo-intro`, `map-over`, `map-over-id`, `map-over-comp`, `map-square`)
| `Smoke.agda` (using-clause for each symbol)
| Pinning is enforced by `Smoke.agda`'s `using` list per CLAUDE.md "Working rules".

| S-017
| readme-status
| non-injectivity/no-section family is present (`collapse-non-injective`, `no-section-collapse`, `no-section-visible`, `no-section-collapse-to-residue`, `no-section-weaken`)
| `EchoCharacteristic.collapse-non-injective` + `EchoResidue.no-section-collapse` + `EchoResidue.no-section-visible` + `EchoResidue.no-section-collapse-to-residue` + `EchoLinear.no-section-weaken`
| Five named symbols; mapping is symbol-for-symbol.

| S-018
| readme-status
| distinct-witness and retained-constraint exemplars are present (`echo-true≢echo-false`, `stateA≢stateB`, `visible-constraint`)
| `EchoCharacteristic.echo-true≢echo-false` + `EchoCharacteristic.stateA≢stateB` (or example file) + `EchoCharacteristic.visible-constraint`
| `visible-constraint` retains expository status only per the Gate 2 amendment (R-2026-05-27).

| S-019
| readme-status
| degrade-law family lands across decoration layers (graded, linear/affine, choreographic, access, cost, search)
| `EchoGraded.degrade-compose` + `EchoLinear.degradeMode-compose` + `EchoChoreo.applyChoreo-compose` + `EchoAccess` + `EchoCost` + `EchoSearch`
| Six decoration layers named; per-layer headlines listed in `docs/theorem-index.md`.

| S-020
| readme-status
| Pillars A–D of the establishment plan are LANDED (with R-2026-05-18 narrowings; see `docs/retractions.adoc`); Pillar E (paper) is in progress
| `EchoFiberBridge` (A) + `EchoPullback` + `EchoGradedComonad` (B) + `EchoSeparating` (C) + `EchoRelModel` (D)
| The "with R-2026-05-18 narrowings" caveat is load-bearing; pillar-level wording differs from `types-abstract.adoc`.

| S-021
| readme-status
| Pillar F gates F1–F5 all PASSED (F1 graded-comonad-witness; F2 Echo-functor second model; F3 second non-isomorphic grade-monoid; F4 funext-qualified pullback UP; F5 funext-qualified full OFS — F-2026-05-27a)
| F1 → `EchoGradedComonadWitness` (per `earn-back-plan.adoc`) + F2 → `EchoStepNDModelF2` + F3 → second grade-monoid module + F4 → `EchoPullbackUnivF4` + F5 → `EchoOFSUnivF5{,Diag,Iso}`
| F1 wording in the README echoes the gate ledger; consumer should cross-check whether "F1 graded-comonad-witness" reads as the *retracted* graded-comonad framing.

| S-022
| readme-status
| Canonical identity layer + Tier-2 grid + audience surfaces LANDED 2026-05-27 (see "Canonical identity layer" section above + `EchoCanonicalIdentitySuite.agda` for the curated entry point)
| `EchoTotalCompletion` + `EchoOrthogonalFactorizationSystem` + `EchoImageFactorization` + `EchoNoSectionGeneric` + Tier-2 modules + audience modules + `EchoCanonicalIdentitySuite`
| Tier listing is exhaustive in README §"Canonical identity layer".

| S-023
| readme-status
| integration bridge (cross-decoration commutation via the recipe; the recipe is useful as organising vocabulary but does not carry substantive simultaneous integration content — see `docs/EI2_REPORT.adoc`. Distinctness rests on truncation and 2-cell arguments)
| `EchoIntegration` + `docs/EI2_REPORT.adoc`
| Mixed factual + framing claim. "Distinctness rests on truncation and 2-cell arguments" is the Gate 1 distinctness commitment; mapping is `EchoVsQuotient.Sophisticated.equalizer→echo-pair` + `EchoVsGalois.Sophisticated.meet→echo-intersection` + the `echo-not-prop` family.

| S-024
| readme-identity
| This repo is trying to establish echo types as a concept with its own identity. Since `Echo` is built from sigma/fiber machinery, identity will not come from syntax. It must come from role and theorems.
| `roadmap-gates.adoc` (Gate 1+2+3 schedule)
| Positioning + falsifiability statement; no single theorem.

| S-025
| readme-identity
| Distinct phenomenon: structured loss under non-injective computation.
| `EchoCharacteristic.collapse-non-injective` + Gate-1 falsifier exhibits
| Gate-1 headline; exact match to `roadmap-gates.adoc` §"Gate 1".

| S-026
| readme-identity
| Characteristic theorem family: results that are naturally echo-shaped, not just generic sigma lemmas.
| `EchoGraded.degrade-via-join` + `EchoGraded.degrade-compose` + `EchoChoreo.applyChoreo-compose` + `EchoLinear.degradeMode-compose`
| Gate-2 headline; four nominees per `roadmap-gates.adoc` §"Gate 2".

| S-027
| readme-identity
| Canonical examples: cases where echo type is the right explanatory unit.
| `tutorial/region_exit_audit/` + `tutorial/epistemic_erasure/` + `tutorial/provenance_debugging/`
| Gate-3 headline; three Lane-5 walkthroughs.

| S-028
| paper-front
| The thesis of the paper is that the type-theoretic standing that *befits* echo is not the linear/dependent judgmental ladder — that comparison is a category error — but a **loss-graded reindexing modality**: a monotone family indexed by a join-semilattice of loss grades, with a functorial reindexing action.
| `EchoGraded` + `EchoFiberBridge.echo↔fib`
| Thesis statement; the negative ("not linear/dependent") has no module to map; the positive maps to `EchoGraded`.

| S-029
| paper-front
| It is *analogous to*, and motivated by, the coeffect/quantitative lineage, but we are exact about what is and is not mechanised.
| NONE-PROPOSED
| Lineage-positioning sentence; no theorem corresponds.

| S-030
| paper-front
| In particular it is *not* a graded comonad in the Petriček–Orchard–Mycroft / Katsumata sense: no nested functor `D_{r·s} ⇒ D_{r} D_{s}` is formed, and the structure maps are the lattice-bottom coercion and the join-left reindex, not a genuine counit/comultiplication pair.
| `EchoGradedComonad` (the actual records present in the module) + `docs/retractions.adoc` §R-2026-05-18
| Negative claim about absent structure; mapping is to what *is* present (single-grade reindex) versus what would be needed (nested family).

| S-031
| paper-intro
| The contribution is a *characterisation package*, mechanised in Agda under `--safe --without-K` with zero postulates
| repo invariant + `EchoFiberBridge` + `EchoPullback` + `EchoGradedComonad` + `EchoSeparating` + `EchoRelModel`
| Umbrella claim; decomposes across Pillars A–D.

| S-032
| paper-intro
| Pinned identity. `echo↔fib : Echo f y ↔ fiber f y`, definitional, `refl` round-trips (Pillar A).
| `EchoFiberBridge.echo↔fib` + `EchoFiberBridge.echo→fib` + `EchoFiberBridge.fib→echo`
| Direct theorem statement.

| S-033
| paper-intro
| `fiber` is a same-module restatement of the Σ; the bridge is the identity.
| `EchoFiberBridge.fiber` (local definition because stdlib 2.3 has none)
| Definitional fact about the bridge's content.

| S-034
| paper-intro
| Pointwise mediator property. `Echo f y` presented as the pullback of `f` along the point `y : ⊤ → B`; a `SliceHom` *is* a cone (round-trips `refl`).
| `EchoPullback.SliceHom` + `EchoPullback.EchoCone` + `EchoPullback.echo-cone`
| Three named artefacts; round-trip is `refl`.

| S-035
| paper-intro
| `echo-pullback-univ` gives existence and *pointwise* uniqueness `∀ v → m' v ≡ m v`.
| `EchoPullback.echo-pullback-univ`
| Direct theorem.

| S-036
| paper-intro
| We do *not* claim full terminality: `m' ≡ m` is neither stated nor provable here without funext, so this is a funext-relative mediator property, not a universal property in the categorical sense (Pillar B-1, §3).
| `EchoPullbackUnivF4` (the funext-parametrised earn-back) + `docs/retractions.adoc` §R-2026-05-18 + §F-2026-05-18a
| Scope-narrowing claim; the earn-back module is the conditional restorer.

| S-037
| paper-intro
| Loss-graded reindexing modality. A monotone family `GEcho` over the loss-grade join-semilattice with a functorial reindex `degrade`.
| `EchoGraded.GEcho` + `EchoGraded.degrade`
| Direct module structure.

| S-038
| paper-intro
| The "counit" is the identity coercion (the bottom grade `keep` is absorbed definitionally); the "comultiplication" is the join-left reindex `GEcho g₁ → GEcho (g₁ ⊔ g₂)` — a *single* grade application, *not* a nested `D_r D_s`.
| `EchoGradedComonad.gextract` + `EchoGradedComonad.gduplicate`
| Pair-symbol mapping; the negative ("not nested") has no in-repo artefact, matched-negative discipline applies.

| S-039
| paper-intro
| The three coherence equations (§4.2) hold for exactly one reason: the grade order is thin (`≤g-prop`) and `degrade` is functorial (`degrade-compose`).
| `EchoGraded.≤g-prop` + `EchoGraded.degrade-compose` + `EchoGradedComonad.gcomonad-counit-l` + `EchoGradedComonad.gcomonad-counit-r` + `EchoGradedComonad.gcomonad-coassoc`
| Five theorems; the three coherence equations + the two load-bearing antecedents.

| S-040
| paper-intro
| This is a thin-poset action, not a graded comonad (Pillar B-2).
| `EchoGradedComonad` (entire module) + `docs/retractions.adoc` §R-2026-05-18
| Reframing claim; the module retains its name despite the narrowing.

| S-041
| paper-intro
| Separating model. the model minus its single hypothesis `≤g-prop`: generic Σ-functoriality survives, the characteristic composition law is refuted at a decidable witness.
| `EchoSeparating.sep-order-not-prop` + `EchoSeparating.sep-map-over-id` + `EchoSeparating.sep-map-over-comp` + `EchoSeparating.sep-degrade-compose-fails`
| Four headlines pinned in `Smoke.agda`.

| S-042
| paper-intro
| Carrier-parametric abstraction (not model-independence). an abstract interface `GradedLossModel` whose `⊑-prop` field *fixes* the only load-bearing hypothesis; the coherence equations are proved once, parametric in the carrier, over a *fixed* grade poset.
| `EchoRelModel.GradedLossModel` + `EchoRelModel.GCLaws`
| Direct module structure; the negative ("not model-independence") flags the R-2026-05-18 narrowing.

| S-043
| paper-intro
| The "second model" is the first with a `⊤` component, agreeing definitionally (`refl`).
| `EchoRelModel.set-model` + `EchoRelModel.rel-model` + `EchoRelModel.model-agreement` + `EchoRelModel.bridge-natural`
| Two-model construction; `refl`-agreement is the narrowed claim.

| S-044
| paper-intro
| Postulate-free build (not a conservativity metatheorem). the whole development typechecks `--safe --without-K` with zero postulates / zero escape pragmas, CI-enforced.
| `scripts/check-no-unsafe.sh` + `scripts/kernel-guard.sh` + `.github/workflows/agda.yml`
| Build-invariant claim; not a theorem.

| S-045
| paper-intro
| With `Echo = Σ` definitionally this is *evidence for* conservativity over MLTT+Σ; it is not a mechanised proof-theoretic conservativity theorem, and is no longer stated as one (§6.2).
| `docs/echo-types/conservativity.adoc` + `docs/retractions.adoc` §R-2026-05-18
| Doc anchor; no theorem statement.

| S-046
| paper-intro
| Methodology. falsifiable gates with explicit retraction actions; identity claims settled negatively and logged, including this development's own framing retraction (§7).
| `roadmap-gates.adoc` + `docs/retractions.adoc`
| Process claim; no single theorem.

| S-047
| paper-intro
| `Echo` introduces no new judgement, context structure, or formation rule — it is a derived Σ-type.
| `Echo.Echo` (the definitional shape)
| Negative claim about absent structure; the positive side maps to the Σ-shape.

| S-048
| paper-intro
| Presenting bespoke "echo introduction/elimination rules" as primitive would be the fastest way to lose an informed reader.
| NONE-PROPOSED
| Editorial framing; no theorem corresponds.

| S-049
| abstract-context
| Define, for `f : A → B` and `y : B`, `Echo f y := Σ (x : A) , (f x ≡ y)`.
| `Echo.Echo`
| Definitional restatement (cf. S-008).

| S-050
| abstract-context
| This is *definitionally* the homotopy fibre `fib f y` (HoTT book Def. 4.2.4); the bridge `echo↔fib` is mechanised with `refl` round-trips.
| `EchoFiberBridge.echo↔fib`
| Restates S-032 in the TYPES-abstract surface.

| S-051
| abstract-context
| We do **not** claim echo as a new judgmental type strength.
| NONE-PROPOSED
| Negative positioning statement.

| S-052
| abstract-context
| The strength that *does* befit echo is a **loss-graded reindexing modality**: a monotone family over a join-semilattice of loss grades with a functorial reindexing action.
| `EchoGraded.GEcho` + `EchoGraded.degrade`
| Restates S-037.

| S-053
| abstract-context
| It is *motivated by* the coeffect / quantitative lineage … but is explicitly *not* a graded comonad in that sense: no nested `D_{r·s} ⇒ D_{r} D_{s}` is formed.
| `EchoGradedComonad` (the actual content) + `docs/retractions.adoc` §R-2026-05-18
| Restates S-030.

| S-054
| abstract-contributions
| `echo↔fib : Echo f y ↔ fiber f y`, definitional, `refl` round-trips, where `fiber` is a same-module restatement of the Σ.
| `EchoFiberBridge.echo↔fib` + `EchoFiberBridge.fiber`
| Restates S-032 + S-033.

| S-055
| abstract-contributions
| `echo-pullback-univ` gives existence and *pointwise* uniqueness `∀ v → m' v ≡ m v`.
| `EchoPullback.echo-pullback-univ`
| Restates S-035.

| S-056
| abstract-contributions
| Earned back conditionally (F4, F-2026-05-18a): strict terminality is mechanised as a function of an explicit `funext` parameter (`EchoPullbackUnivF4.agda`, not a postulate); the unconditional claim stays retracted, the pointwise corollary is kept.
| `EchoPullbackUnivF4` + `docs/retractions.adoc` §F-2026-05-18a
| F4 earn-back; the module-level `funext` is a parameter, not a `postulate` declaration.

| S-057
| abstract-contributions
| Over the loss-grade join-semilattice `(Grade, ⊔, keep)`: `gextract` is the identity coercion (`keep` is the definitional bottom), `gduplicate` is the join-left *single-grade* reindex.
| `EchoGradedComonad.gextract` + `EchoGradedComonad.gduplicate` + `EchoGraded.keep` + `EchoGraded._⊔g_`
| Restates S-038.

| S-058
| abstract-contributions
| In the common-upper-bound idiom the three coherence equations collapse to `degrade-compose` plus propositionality of the loss order (`≤g-prop`), with no `subst`/transport.
| `EchoGradedComonad.gcomonad-counit-l` + `EchoGradedComonad.gcomonad-counit-r` + `EchoGradedComonad.gcomonad-coassoc` + `EchoGraded.degrade-compose` + `EchoGraded.≤g-prop`
| Restates S-039.

| S-059
| abstract-contributions
| The characteristic composition equation is *refuted* at a concrete decidable witness (checked `true ≢ false`).
| `EchoSeparating.sep-degrade-compose-fails`
| Direct symbol.

| S-060
| abstract-contributions
| `GradedLossModel`'s `⊑-prop` field *fixes* the only load-bearing hypothesis; `GCLaws` proves the equations once, parametric in the carrier, over a *fixed* grade poset.
| `EchoRelModel.GradedLossModel` + `EchoRelModel.GCLaws`
| Restates S-042.

| S-061
| abstract-contributions
| Earned back, scoped (F2, F-2026-05-18a): a genuine second model of the *bare Echo functor* (not the modality) exists on the non-graph relation `StepND` (`EchoStepNDModelF2.agda`; `nd-not-graph` checked), with content-bearing agreement — not Σ-η on `× ⊤`.
| `EchoStepNDModelF2` (including `nd-not-graph`) + `docs/retractions.adoc` §F-2026-05-18a
| F2 earn-back; the negative "not Σ-η on × ⊤" is a deliberate matched-negative.

| S-062
| abstract-discipline
| The Agda development is `--safe --without-K` throughout, with **zero postulates and zero escape pragmas**, CI-enforced — a build property that is *evidence for*, not a proof of, conservativity.
| `scripts/check-no-unsafe.sh` + `scripts/kernel-guard.sh` + `.github/workflows/agda.yml`
| Restates S-044 with the narrowed conservativity reading.

| S-063
| abstract-discipline
| Every headline theorem is pinned in a `Smoke.agda` manifest so a silent rename fails CI fast.
| `Smoke.agda` + `tutorial/*/Smoke.agda` + `Ordinal/Buchholz/Smoke.agda`
| Smoke-pin invariant; CLAUDE.md "Working rules" enforces it per change.

| S-064
| abstract-discipline
| There is no funext anywhere; the genuine funext-relativity is the pointwise-only mediator property, disclosed as a bound rather than presented as a "funext-free" feature.
| `EchoPullback.echo-pullback-univ` (no funext) + `EchoPullbackUnivF4` (funext as parameter)
| Disclosure claim; the load-bearing artefact is the absence of a `postulate funext` line tree-wide.

| S-065
| abstract-position
| Echo sits beside, not within, the linear/dependent ladder.
| NONE-PROPOSED
| Positioning statement; no theorem.

| S-066
| abstract-position
| Echo *is* the fibre (`echo↔fib`, `refl` round-trips). The win is not against HoTT but one step to the side: against *propositionally-truncated* fibres, where `‖ Echo f y ‖` discards witness identity, the un-truncated form retains it (`echo-not-prop` certificates).
| `EchoFiberBridge.echo↔fib` + the `echo-not-prop` family in `proofs/agda/examples/{TropicalArgmin,EpistemicUpdate,LinearErasure}.agda`
| Truncation argument — the Gate-1 structural lever.

| S-067
| abstract-position
| Echo is *motivated by* this lineage but explicitly *not* an instance — `EchoGradedComonad.agda` exhibits no nested `D_r D_s`. What we provide is a *calibration*: the separating model isolates the single load-bearing hypothesis (`≤g-prop`) at which generic Σ-functoriality stops being enough.
| `EchoGradedComonad` (content audit) + `EchoSeparating.sep-degrade-compose-fails`
| Restates S-030 + S-041 under the "calibration" framing.

| S-068
| build-card
| (no card strings located)
| NONE-PROPOSED
| `build-card.mjs` does not exist in this repo or any sibling repo under `/home/hyperpolymath`. Row retained as an explicit `LOCATE-FAILED` placeholder.

| S-069
| linkedin
| (draft copy not located)
| NONE-PROPOSED
| LinkedIn draft not found by file-name or content search. Row retained as an explicit `LOCATE-FAILED` placeholder so a future session can supply the verbatim sentences without disturbing earlier IDs.
|===

== Phrase sweep — "graded comonad", "genuinely different"

Literal-string sweep over the in-scope surfaces *plus* `CLAUDE.md`,
per the brief. Hits on retracted/legacy documents (`docs/retracted/`)
are excluded except where they are the only surface that carries the
phrase.

[cols="1,1,1,4,3,3", options="header"]
|===
| ID | Phrase | Surface (file:line) | Verbatim sentence (or quoted span) | Proposed mapping | Note

| P-001
| graded comonad
| paper.adoc:59
| "it is *not* a graded comonad in the Petriček–Orchard–Mycroft / Katsumata sense"
| `EchoGradedComonad` (content) + `docs/retractions.adoc` §R-2026-05-18
| Negative use; cf. S-030.

| P-002
| graded comonad
| paper.adoc:73
| "The earlier framing of this development as a 'graded comonad with two independent models and a conservativity metatheorem' was retracted on 2026-05-18"
| `docs/retractions.adoc` §R-2026-05-18
| Self-referential retraction citation.

| P-003
| graded comonad
| paper.adoc:111
| "This is a thin-poset action, not a graded comonad (Pillar B-2)."
| `EchoGradedComonad` (content) + `EchoGraded.degrade-compose`
| Negative use; cf. S-040.

| P-004
| graded comonad
| paper.adoc:631
| "B (graded comonad)" — Pillar-listing row label inside the §"Evaluation and discussion" comparator table
| `EchoGradedComonad` (module name) + `docs/echo-types/earn-back-plan.adoc` §"Gate F1"
| Table-label use; not a fresh claim but echoes the retracted nomenclature.

| P-005
| graded comonad
| paper.adoc:698
| "what a genuine graded comonad in the Petriček–Orchard–Mycroft sense"
| NONE-PROPOSED
| Comparator wording inside §"Evaluation and discussion".

| P-006
| graded comonad
| paper.adoc:704
| "does not exhibit a graded comonad in that sense — that claim was [retracted]"
| `docs/retractions.adoc` §R-2026-05-18
| Restates S-030 inside the evaluation section.

| P-007
| graded comonad
| paper.adoc:884, 915–916
| §"Graded comonads, coeffects, and QTT (the lineage we are motivated by)" — header + body "sits one rung *below* a graded comonad"
| `EchoGradedComonad` + `EchoGraded`
| Related-work section; positions Echo strictly below the graded-comonad structure in the comparator ladder.

| P-008
| graded comonad
| paper.adoc:1188, 1211
| "has been delivered above in §… §'Graded comonads, …'" + table row "'graded comonad' (counit/comultiplication, three comonad laws)"
| `EchoGradedComonad.gextract` + `EchoGradedComonad.gduplicate` + `EchoGradedComonad.gcomonad-{counit-l,counit-r,coassoc}`
| Cross-reference + claim-list table row.

| P-009
| graded comonad
| types-abstract.adoc:16
| "This abstract previously claimed a graded comonad, two independent models, a conservativity metatheorem and a terminal-cone universal property."
| `docs/retractions.adoc` §R-2026-05-18
| Self-referential retraction citation.

| P-010
| graded comonad
| types-abstract.adoc:82
| "this is a thin-poset action, not a graded comonad."
| `EchoGradedComonad` (content)
| Restates S-040 in the abstract.

| P-011
| graded comonad
| types-abstract.adoc:136
| "explicitly not a graded comonad — it is an *exactly-bounded package*"
| `EchoGradedComonad` (content) + `EchoSeparating`
| Restates the positive reframing.

| P-012
| graded comonad
| types-abstract.adoc:152
| §"Graded comonads / coeffects / QTT (Uustalu–Vene; Petriček et al.; …)" related-work heading
| `EchoGradedComonad`
| Related-work header; not a fresh claim.

| P-013
| graded comonad
| README.md (sweep result: no hit)
| (no occurrence)
| n/a
| Confirms the README has been swept of the retracted-framing phrase.

| P-014
| graded comonad
| gh-desc (sweep result: no hit)
| (no occurrence)
| n/a
| Confirms the GitHub description has been swept.

| P-015
| graded comonad
| CLAUDE.md:91
| "Path to recognised type-theoretic standing as a *characterised graded comonad of structured loss* (coeffect/quantitative lineage)"
| `docs/echo-types/establishment-plan.adoc`
| **Framing risk flagged** — this restates the retracted graded-comonad framing as the active "path to standing". See §"CLAUDE.md framing assessment" below.

| P-016
| graded comonad
| CLAUDE.md:94
| "B universal property + graded-comonad laws"
| `EchoGradedComonad` (the actual gcomonad-counit-*/coassoc names) + `docs/retractions.adoc` §R-2026-05-18
| **Framing risk flagged** — uses retracted nomenclature for Pillar B inside the ledger.

| P-017
| graded comonad
| CLAUDE.md:1830
| "Path to recognised type-theoretic standing as a characterised *graded comonad of structured loss*"
| `docs/echo-types/establishment-plan.adoc` (legacy entry)
| **Framing risk flagged** — duplicate of P-015 inside the original establishment-track entry; predates the R-2026-05-18 narrowing and was never rewritten.

| P-018
| graded comonad
| CLAUDE.md:1899
| "Background/notation primer (graded comonads + HoTT fibres)"
| NONE-PROPOSED
| Plan-language only; not a claim about what is present in the artefact.

| P-019
| genuinely different
| paper.adoc / types-abstract.adoc / README.md / gh-desc (sweep result: no hit on any in-scope surface)
| (no occurrence)
| n/a
| Phrase does not appear on any in-scope surface. The phrase does appear in non-scope surfaces (`docs/gate-3-handoff.adoc:60`, `docs/adjacency/provenance-semirings.adoc:103`, `docs/echo-types/explorations/decoration-bridge/01-decoration-instances.adoc:206`) — recorded here for traceability only; not part of Gate G1 input.

| P-020
| genuinely different
| CLAUDE.md (sweep result: no hit)
| (no occurrence)
| n/a
| Phrase absent from CLAUDE.md.
|===

== CLAUDE.md framing assessment

The literal sweep flags three CLAUDE.md occurrences of "graded comonad"
in framing positions (P-015, P-016, P-017) that an independent
auditor reading CLAUDE.md *first* — before reading
`docs/retractions.adoc` — could read as the active framing of the
establishment track. Specifically:

* CLAUDE.md:89–91 frames the establishment track as the path to
  standing "as a characterised graded comonad of structured loss",
  without an inline retraction marker.
* CLAUDE.md:93–95 lists Pillar B as "universal property +
  graded-comonad laws" inside the active workstream summary.
* CLAUDE.md:1828–1833 carries the original 2026-05-17 entry under
  the heading "Establishment-track opening rung" with the same
  graded-comonad framing, also without an inline retraction marker
  (the entry predates R-2026-05-18 by one day).

This file therefore proposes a minimal `CLAUDE.md.audit` variant —
a strict subset of CLAUDE.md with these three spans annotated or
re-phrased per the R-2026-05-18 narrowing, intended for use by an
independent auditor reading CLAUDE.md as ambient context for Gate
G1. The variant is *not* a rewrite of the session ledger; it
preserves every other word, line, and session-arc verbatim, and
only adjusts the three framing spans.

The variant lives at `/home/hyperpolymath/developer/repos/echo-types/CLAUDE.md.audit`
and is built by inserting an inline `[RETRACTED 2026-05-18: now stated
as a *loss-graded reindexing modality* — see docs/retractions.adoc
§R-2026-05-18]` marker after each of the three flagged framings.
No content is deleted; the bias-reducing edit is additive.

== Provenance and follow-up

* Constructor brief executed verbatim except for the absent surfaces
  noted in §"Scope and absent surfaces". Each absent surface keeps
  an explicit row (S-068 / S-069) so a downstream session can fill
  it without renumbering.
* This file is mapping-only. No claim above is judged justified or
  unjustified; that judgement is the next gate session's
  responsibility.
* All `(module, theorem)` proposals correspond to symbols present
  on the current `session/slice-4-narrowing` branch as of
  2026-06-10. Branch-divergence handling is left to the consumer.
